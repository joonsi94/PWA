function solution(n) {
    var answer = 0;
    for (let i = 0; i < n.length; i++) {
        n.length[i] = n.length[i]
        answer = n.length[i]
    }
    return answer;
}

const res = solution([1, 2, 3, 4, 5])
console.log(res)