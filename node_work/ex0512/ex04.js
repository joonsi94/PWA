const cryto=require('crypto');

console.log(cryto.createHash('sha256'),update('hello wordp').digest(hex));
console.log(cryto.createHash('sha256'),update('hello wordp').digest('base64'));

console.log(cryto.dandomUUID())